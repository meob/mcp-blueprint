name: get_slow_queries
description: 'Return the statements with the highest total execution time, with call counts and average/maximum duration, ordered by cumulative time.'
sql: ../sql/get_slow_queries.sql
cache:
  ttl: 30
