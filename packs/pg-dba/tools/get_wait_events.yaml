name: get_wait_events
description: Return the wait event distribution across active sessions.

parameters:
  database:
    type: string
    required: false
    description: Optional database name filter.

sql: ../sql/get_wait_events.sql

cache:
  ttl: 15
