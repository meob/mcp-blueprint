name: get_database_size
description: Return the size of every database in the cluster.

parameters:
  database:
    type: string
    required: false
    description: Optional database name filter.

sql: ../sql/get_database_size.sql

cache:
  ttl: 30

format:
  rename:
    size_bytes: size
  convert_size:
    - size
