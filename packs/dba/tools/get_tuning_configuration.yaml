name: get_tuning_configuration
description: >-
  Return the most relevant tuning parameters (buffers, redo log,
  replication, concurrency, timeouts) with their current value and unit,
  ordered by name.
sql:
  postgresql: ../sql/postgresql/get_tuning_configuration.sql
  mysql: ../sql/mysql/get_tuning_configuration.sql
cache:
  ttl: 300
