name: get_active_sessions
description: Return sessions that are currently executing a query.

parameters:
  database:
    type: string
    required: false
    description: Optional database name filter.

sql: ../sql/get_active_sessions.sql

cache:
  ttl: 5
